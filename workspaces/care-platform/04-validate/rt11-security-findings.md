# RT11 — Security Audit Findings

**Date**: 2026-03-14
**Scope**: All Phase 3 source files
**Reviewer**: security-reviewer agent (two independent runs)

---

## CRITICAL — Resolved

| ID  | Finding                               | File            | Status                                                                   |
| --- | ------------------------------------- | --------------- | ------------------------------------------------------------------------ |
| C1  | Live API keys in `.env`               | `.env`          | **User action** — rotate keys. Never committed (`.gitignore` covers it). |
| C2  | 7 GET endpoints lacked authentication | `api/server.py` | **FIXED** — All endpoints now require `Depends(verify_token)`.           |

## HIGH — Tracked for Phase 4

| ID  | Finding                                                          | File                                    | Rationale for deferral                                                         |
| --- | ---------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------ |
| H1  | Prompt injection surface — `task.action` passed directly to LLM  | `execution/kaizen_bridge.py:150`        | Inherent to agent execution; system prompt hardening planned for Phase 4       |
| H2  | Keyword bypass in consequential action detection (case, unicode) | `execution/posture_enforcer.py:311-327` | Known limitation of heuristic approach; CARE expert confirmed as MEDIUM        |
| H3  | MemoryStore/FilesystemStore lack thread safety                   | `persistence/store.py`                  | Testing/dev stores only — production uses SQLite/PostgreSQL (both thread-safe) |
| H4  | No security response headers (CSP, X-Frame-Options)              | `api/server.py`                         | API server, not browser-served; headers added in Phase 4 deployment hardening  |
| H5  | No rate limiting on API endpoints                                | `api/server.py`                         | `slowapi` planned for Phase 4                                                  |
| H6  | `verify_token` returned raw token                                | `api/server.py:151`                     | **FIXED** — now returns `"authenticated"`                                      |
| H7  | Readiness probe leaked internal error details                    | `api/server.py:191`                     | **FIXED** — generic message returned                                           |
| H8  | `cost_report` `days` unbounded                                   | `api/server.py:249`                     | **FIXED** — `ge=1, le=365` added                                               |

## MEDIUM — Tracked

| ID  | Finding                                         | Status                                    |
| --- | ----------------------------------------------- | ----------------------------------------- |
| M1  | EventBus subscriber_count race condition        | asyncio single-threaded; near-zero impact |
| M2  | ApprovalQueue expire_old reentrancy             | No current deadlock path                  |
| M3  | .env parser truncates values with ` #`          | Documented limitation                     |
| M4  | PostgreSQL `_drop_all_tables` uses f-string SQL | Hardcoded table names only                |
| M5  | MemoryStore genesis write-once                  | **FIXED**                                 |
| M6  | FilesystemStore genesis write-once              | **FIXED**                                 |

## Passed Checks (18)

- No hardcoded secrets in source
- No SQL injection (parameterized queries throughout)
- No eval/exec on user input
- Fail-closed enforcement (BLOCKED/FAILED on error)
- Timing-safe token comparison (hmac.compare_digest)
- Thread-safe task queue, spend tracking, credentials
- Path traversal protection in FilesystemStore
- Frozen constraint envelopes (immutable after construction)
- Ed25519 signing with nonce replay prevention
- Restricted CORS (explicit origins, methods, headers)
- Non-root Docker containers with health checks
- Append-only audit enforcement via database triggers
- Write-once genesis records across all store implementations
- Cascade revocation with orphan detection
- Circuit breaker with fail-safe recovery
- Emergency halt mechanism

---

_Generated by security-reviewer — RT11 round — 2026-03-14_
